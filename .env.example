# API Keys for bearer authentication (comma-separated for multiple keys)
API_KEYS=your-secret-api-key-here,another-key-if-needed

# Set to 1 to disable authentication (development only)
DISABLE_AUTH=0
